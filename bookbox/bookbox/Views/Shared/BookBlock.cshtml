@model Book

<div class="col-sm-4 col-lg-4 col-md-4">
    <div class="thumbnail">
        <img src="@Model.PicturePath" alt="" width="200">
        <div class="caption">
            <h3>
                <a asp-controller="Book" asp-action="Details" asp-route-id="@Model.BookId">@Model.Title</a>
            </h3>
            <p>@Model.Author.Name @Model.Author.LastName</p>
        </div>
        <div class="addToCart">
            @*<p class="button">
                <a class="btn btn-primary" asp-controller="ShoppingCart" asp-action="AddToShoppingCart"
                   asp-route-pieId="@Model.PieId">Add to cart</a>
            </p>*@
        </div>
    </div>
</div>