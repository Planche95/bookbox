@if (SignInManager.IsSignedIn(User) && User.IsInRole("Admin"))
{
    <a asp-action="Edit" asp-route-id="@Model.BookId" class="btn btn-default">Edit</a>
    <a asp-action="Delete" asp-route-id="@Model.BookId" class="btn btn-default">Delete</a>
}